# Examen-01

Estructura inicial del proyecto clonada desde `Clase-003`.

Archivos incluidos:
- `index.html` — punto de entrada HTML
- `main.js` — script principal (vacío por ahora)
- `package.json` — scripts y dependencias
- `assets/models/` — modelos 3D u otros
- `assets/textures/` — texturas

Para arrancar el servidor local (tras instalar dependencias):

```
cd "Examen-01"
npm run start
```
